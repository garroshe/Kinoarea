import styled from "styled-components";

export const StyledSocial = styled.div`
  display: flex;
  align-items: center;
  gap: 25px;

  svg {
    max-height: 15px;
    max-width: 15px;
    cursor: pointer;
  }
`;
