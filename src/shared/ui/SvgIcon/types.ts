export type IconsPropsType = {
  className?: string;
  onClick?: () => void;
  color?: string;
};

export type SvgIconPropsType = {
  className?: string;
  onClick?: () => void;
  icon: string;
  color?: string;
};
