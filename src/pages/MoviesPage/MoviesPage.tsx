import { Header } from "@/widgets/Header";
import { MovieList } from "@/widgets/MoviesList";

const MoviesPage = () => {
  return (
    <>
      <Header />
      <MovieList />
    </>
  );
};

export default MoviesPage;
